{
  "name": "@sparkloom/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply sparkloom-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply sparkloom-db --remote"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@sparkloom/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}

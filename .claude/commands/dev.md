---
description: Launch SparkLoom in development mode
---

Start the full development environment:

1. Run `pnpm install` in the monorepo root if node_modules is missing
2. Run `pnpm tauri:dev` to start both the Vite dev server and the Tauri app
3. Watch for compilation errors and fix them
4. Report the status when the app window appears
